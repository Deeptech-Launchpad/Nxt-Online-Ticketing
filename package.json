{
  "name": "nxt-online-ticketing",
  "version": "1.0.0",
  "description": "Master launcher for AltiusNXT Online Ticketing system",
  "scripts": {
    "dev": "concurrently \"cd helpdesk-backend && npm run dev\" \"cd helpdesk-react && npm run dev\"",
    "install-all": "npm install && cd helpdesk-backend && npm install && cd ../helpdesk-react && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
